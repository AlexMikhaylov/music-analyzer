# Spring profiles active property determines which profile(s) are active.
# If you don't set it, 'dev' will be used as default for local development.
spring.profiles.active=dev

# Common properties can be defined here, which will be shared among all profiles.


